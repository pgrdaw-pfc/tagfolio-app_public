# Tagfolio – CSS Style Guide

This document describes the visual components, layout rules, and utility
classes used in the Tagfolio web application.

The style guide is generated automatically using **KSS (Knyle Style Sheets)**
from comments embedded in the CSS source files.

## Structure

- **Base**: Resets and base styles
- **Layout**: Page layout components
- **Components**: Reusable UI elements
- **Utilities**: Helper and utility classes
